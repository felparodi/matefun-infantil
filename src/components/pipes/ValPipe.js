import React from 'react';

import ValueBottom from '../../svg/value-bottom.svg';

export class ValPipe extends React.Component {

    render() {
        const { pipe } = this.props;
        return (
            <ValueBottom/>
        )
    }
}

