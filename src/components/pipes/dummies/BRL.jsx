import React from 'react';
import T from './T';

const BRL = (props) => (
    <svg viewBox="0 0 40 40" className={props.className}>
        <T transform="rotate(90 20 20)"/>
    </svg>
);

export default BRL;