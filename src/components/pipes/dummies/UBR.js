import React from 'react';
import T from './T';

const UBR = (props) => (
    <svg viewBox="0 0 40 40" className={props.className}>
        <T transform="rotate(0 20 20)"/>
    </svg>
);

export default UBR;