import React from 'react';
import T from './T';

const UBL = (props) => (
    <svg viewBox="0 0 40 40" className={props.className}>
        <T transform="rotate(180 20 20)"/>
    </svg>
);

export default UBL;